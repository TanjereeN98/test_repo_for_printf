#include <strarg.h>

/**
 * char_format - function to format chars
 * @list: args to the function
 * Return:
 */

void char_format(va_list list)
{
	definition
}

/**
 * char_format - function to format chars
 * @list: args to the function
 * Return:
 */

void string_format(va_list list)
{
	definition
}

/**
 * char_format - function to format chars
 * @list: args to the function
 * Return:
 */

void int_format(va_list list)
{
	definition
}

